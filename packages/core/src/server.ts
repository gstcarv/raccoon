// Entry point — binds port and starts the HTTP server.
import process from "node:process";

// TODO: wire up after Phase 3 (config) and Phase 4 (HTTP layer)
process.stdout.write("raccoon server starting...\n");
