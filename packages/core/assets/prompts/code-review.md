# Code Review

Review the pull request diff for:

1. Correctness — does the change do what is described?
2. Edge cases — are error paths handled?
3. Security — no secrets, no injections, no unsafe patterns
4. Simplicity — is there a simpler approach?
5. Tests — is the new behaviour covered?

Return a structured review with APPROVE, REQUEST_CHANGES, or COMMENT.
